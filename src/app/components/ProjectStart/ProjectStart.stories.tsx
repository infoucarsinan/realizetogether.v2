import React from "react";
import ProjectStart from "./ProjectStart";

export default {
  title: "Component/ProjectStart",
  component: ProjectStart,
};

export const ProjectStartProject = (): JSX.Element => <ProjectStart />;
